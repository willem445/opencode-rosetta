/**
 * STUB (S1) -- replaced by S4 (`feat/copilot-instructions-prompts-skills`).
 * B6: `.github/prompts/**\/*.prompt.md` -> `cfg.command[name]`.
 */
import type { Ctx } from "../../context.js";
import { emptyFragment, type Fragment } from "../types.js";

export function copilotPrompts(_ctx: Ctx): Fragment {
  return emptyFragment();
}
