/**
 * STUB (S1) -- replaced by S5 (`feat/copilot-agents-release`).
 * B7: `.github/agents/**\/*.agent.md` (+ `*.chatmode.md`), `~/.copilot/agents` -> `cfg.agent[name]`.
 */
import type { Ctx } from "../../context.js";
import { emptyFragment, type Fragment } from "../types.js";

export function copilotAgents(_ctx: Ctx): Fragment {
  return emptyFragment();
}
