/**
 * STUB (S1) -- replaced by S3 (`feat/mcp-claude-copilot`).
 * B3: `.mcp.json`, `~/.claude.json` -> `cfg.mcp[name]`.
 */
import type { Ctx } from "../../context.js";
import { emptyFragment, type Fragment } from "../types.js";

export function claudeMcp(_ctx: Ctx): Fragment {
  return emptyFragment();
}
